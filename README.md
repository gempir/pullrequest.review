# pullrequest.review

A lightweight Bitbucket pull request review app with a terminal-inspired UI, focused on fast diff reading, file-tree navigation, and keyboard-driven workflows.

To install dependencies:

```bash
bun install
```

To start a development server:

```bash
bun dev
```
